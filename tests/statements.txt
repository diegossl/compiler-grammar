function int f() {
    FuncCall
    VarBlock
    VarAssign
    print
    read
    return GenExpr;
          
    if(LogExpr) then {
        VarBlock
        
        while(LogExpr) {
            if(LogExpr) then {
                read
                print
            } else
                read
        }
    } else if(LogExpr) then {
    
        if(LogExpr) then
            return GenExpr;
            
        else if (LogExpr) then
            return GenExpr;
            
        else if (LogExpr) then
            return GenExpr;
    }
     
     while(LogExpr);   
     if(LogExpr) then;
     return GenExpr;
}

procedure p(){
    FuncCall
    VarBlock
    VarAssign
    print
    read
          
    if(LogExpr) then {
        VarBlock
        
        while(LogExpr) {
            if(LogExpr) then {
                read
                print
            } else
                read
        }
    } else if(LogExpr) then {
    
        if(LogExpr) then
            VarBlock
        else if (LogExpr) then
            FuncCall
            
        else if (LogExpr) then
            print
    }
     
     while(LogExpr);   
     if(LogExpr) then;
}
